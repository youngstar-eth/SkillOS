═══════════════════════════════════════════════════════════════
  MAS — Farcaster Account Association Walkthrough (20 games)
═══════════════════════════════════════════════════════════════

You need to sign a proof-of-domain ownership for each game's Vercel
subdomain. Your Farcaster custody wallet is the signer. Once signed,
the 3 returned fields (header/payload/signature) go into Vercel env.

The deploy step already set everything else — only account
association is left.

General steps for each domain:
  1. Open https://farcaster.xyz/~/developers/mini-apps/manifest
  2. Paste the domain shown below (WITHOUT https://)
  3. Click "Submit" → "Verify"
  4. Approve the signature request in Warpcast
  5. Copy the three values shown (header / payload / signature)
  6. Run the three `vercel env add` commands
  7. Trigger a redeploy so the manifest picks up the new env vars

Per-game instructions below. Estimated: ~1 min signing + ~30s env ops
per game × 20 games = ~30 min total.

───────────────────────────────────────────────────────────────

[2048]
  Domain:       mas-2048.vercel.app
  Manifest URL: https://mas-2048.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-2048.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/2048
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[wordle]
  Domain:       mas-wordle.vercel.app
  Manifest URL: https://mas-wordle.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-wordle.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/wordle
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[snake]
  Domain:       mas-snake.vercel.app
  Manifest URL: https://mas-snake.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-snake.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/snake
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[minesweeper]
  Domain:       mas-minesweeper.vercel.app
  Manifest URL: https://mas-minesweeper.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-minesweeper.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/minesweeper
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[sudoku]
  Domain:       mas-sudoku.vercel.app
  Manifest URL: https://mas-sudoku.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-sudoku.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/sudoku
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[pong]
  Domain:       mas-pong.vercel.app
  Manifest URL: https://mas-pong.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-pong.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/pong
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[clicker]
  Domain:       mas-clicker.vercel.app
  Manifest URL: https://mas-clicker.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-clicker.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/clicker
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[breakout]
  Domain:       mas-breakout.vercel.app
  Manifest URL: https://mas-breakout.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-breakout.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/breakout
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[bubble]
  Domain:       mas-bubble.vercel.app
  Manifest URL: https://mas-bubble.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-bubble.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/bubble
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[solitaire]
  Domain:       mas-solitaire.vercel.app
  Manifest URL: https://mas-solitaire.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-solitaire.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/solitaire
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[match3]
  Domain:       mas-match3.vercel.app
  Manifest URL: https://mas-match3.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-match3.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/match3
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[flappy]
  Domain:       mas-flappy.vercel.app
  Manifest URL: https://mas-flappy.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-flappy.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/flappy
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[crossy]
  Domain:       mas-crossy.vercel.app
  Manifest URL: https://mas-crossy.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-crossy.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/crossy
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[helix]
  Domain:       mas-helix.vercel.app
  Manifest URL: https://mas-helix.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-helix.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/helix
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[geometry]
  Domain:       mas-geometry.vercel.app
  Manifest URL: https://mas-geometry.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-geometry.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/geometry
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[jetpack]
  Domain:       mas-jetpack.vercel.app
  Manifest URL: https://mas-jetpack.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-jetpack.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/jetpack
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[stickman]
  Domain:       mas-stickman.vercel.app
  Manifest URL: https://mas-stickman.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-stickman.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/stickman
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[tower]
  Domain:       mas-tower.vercel.app
  Manifest URL: https://mas-tower.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-tower.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/tower
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[pool]
  Domain:       mas-pool.vercel.app
  Manifest URL: https://mas-pool.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-pool.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/pool
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

[hillclimb]
  Domain:       mas-hillclimb.vercel.app
  Manifest URL: https://mas-hillclimb.vercel.app/.well-known/farcaster.json

  1. Paste into https://farcaster.xyz/~/developers/mini-apps/manifest:
       mas-hillclimb.vercel.app
  2. Sign with your Warpcast custody wallet.
  3. Copy the 3 returned values and run:

       cd apps/hillclimb
       printf '%s\n' '<HEADER_VALUE>'    | npx vercel@latest env add FARCASTER_HEADER    production
       printf '%s\n' '<PAYLOAD_VALUE>'   | npx vercel@latest env add FARCASTER_PAYLOAD   production
       printf '%s\n' '<SIGNATURE_VALUE>' | npx vercel@latest env add FARCASTER_SIGNATURE production
       npx vercel@latest --prod --yes

───────────────────────────────────────────────────────────────

After all 20 are signed and redeployed, verify with:

  for g in 2048 wordle snake minesweeper sudoku pong clicker breakout \
           bubble solitaire match3 flappy crossy helix geometry jetpack \
           stickman tower pool hillclimb; do
    code=$(curl -s "https://mas-$g.vercel.app/.well-known/farcaster.json" \
      | python3 -c 'import sys,json; d=json.load(sys.stdin); print("ok" if "accountAssociation" in d else "missing")')
    echo "$g: $code"
  done

Expect: 20 × "ok".
