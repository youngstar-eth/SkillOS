═══════════════════════════════════════════════════════════════
  MAS — Base App Account Association Walkthrough (20 games)
═══════════════════════════════════════════════════════════════

Each mini app needs a signed proof that you own its Vercel
subdomain. Base App (base.dev) issues the signature via your
Coinbase Wallet. Once signed, 4 values go into Vercel env +
one redeploy makes the manifest pick them up.

General steps for each domain:
  1. Open https://base.dev (sign in with Coinbase account)
  2. Navigate: Preview → Account Association
  3. Paste the domain shown below (WITHOUT https://)
  4. Click Submit → Verify → Sign in Coinbase Wallet
     (use the Coinbase Wallet already linked to your Farcaster
     FID if you have one; otherwise any Coinbase Wallet)
  5. Copy the returned object: { header, payload, signature }
     plus the verified signing address (baseBuilder)
  6. Run the 4 `vercel env add` commands from apps/<game>
  7. Redeploy so the manifest picks up the new env vars

Estimated: ~1 min signing + ~30s env ops + ~3 min redeploy
per game × 20 games = ~90 min total (or parallelize redeploys).

───────────────────────────────────────────────────────────────

### GAME: 2048
URL:     https://mas-2048.vercel.app
Manifest: https://mas-2048.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-2048.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/2048):

  cd apps/2048
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-2048.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: wordle
URL:     https://mas-wordle.vercel.app
Manifest: https://mas-wordle.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-wordle.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/wordle):

  cd apps/wordle
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-wordle.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: snake
URL:     https://mas-snake.vercel.app
Manifest: https://mas-snake.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-snake.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/snake):

  cd apps/snake
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-snake.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: minesweeper
URL:     https://mas-minesweeper.vercel.app
Manifest: https://mas-minesweeper.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-minesweeper.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/minesweeper):

  cd apps/minesweeper
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-minesweeper.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: sudoku
URL:     https://mas-sudoku.vercel.app
Manifest: https://mas-sudoku.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-sudoku.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/sudoku):

  cd apps/sudoku
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-sudoku.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: pong
URL:     https://mas-pong.vercel.app
Manifest: https://mas-pong.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-pong.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/pong):

  cd apps/pong
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-pong.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: clicker
URL:     https://mas-clicker.vercel.app
Manifest: https://mas-clicker.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-clicker.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/clicker):

  cd apps/clicker
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-clicker.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: breakout
URL:     https://mas-breakout.vercel.app
Manifest: https://mas-breakout.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-breakout.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/breakout):

  cd apps/breakout
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-breakout.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: bubble
URL:     https://mas-bubble.vercel.app
Manifest: https://mas-bubble.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-bubble.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/bubble):

  cd apps/bubble
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-bubble.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: solitaire
URL:     https://mas-solitaire.vercel.app
Manifest: https://mas-solitaire.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-solitaire.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/solitaire):

  cd apps/solitaire
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-solitaire.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: match3
URL:     https://mas-match3.vercel.app
Manifest: https://mas-match3.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-match3.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/match3):

  cd apps/match3
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-match3.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: flappy
URL:     https://mas-flappy.vercel.app
Manifest: https://mas-flappy.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-flappy.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/flappy):

  cd apps/flappy
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-flappy.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: crossy
URL:     https://mas-crossy.vercel.app
Manifest: https://mas-crossy.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-crossy.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/crossy):

  cd apps/crossy
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-crossy.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: helix
URL:     https://mas-helix.vercel.app
Manifest: https://mas-helix.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-helix.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/helix):

  cd apps/helix
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-helix.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: geometry
URL:     https://mas-geometry.vercel.app
Manifest: https://mas-geometry.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-geometry.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/geometry):

  cd apps/geometry
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-geometry.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: jetpack
URL:     https://mas-jetpack.vercel.app
Manifest: https://mas-jetpack.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-jetpack.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/jetpack):

  cd apps/jetpack
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-jetpack.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: stickman
URL:     https://mas-stickman.vercel.app
Manifest: https://mas-stickman.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-stickman.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/stickman):

  cd apps/stickman
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-stickman.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: tower
URL:     https://mas-tower.vercel.app
Manifest: https://mas-tower.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-tower.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/tower):

  cd apps/tower
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-tower.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: pool
URL:     https://mas-pool.vercel.app
Manifest: https://mas-pool.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-pool.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/pool):

  cd apps/pool
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-pool.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

### GAME: hillclimb
URL:     https://mas-hillclimb.vercel.app
Manifest: https://mas-hillclimb.vercel.app/.well-known/farcaster.json

  1. Open:      https://base.dev
  2. Login:     Coinbase account
  3. Navigate:  Preview → Account Association
  4. Domain:    mas-hillclimb.vercel.app
  5. Submit → Verify → Sign (Coinbase Wallet)
  6. Copy: { header, payload, signature } + verified address

Terminal (from apps/hillclimb):

  cd apps/hillclimb
  echo "<HEADER>"    | /opt/homebrew/bin/npx vercel env add FARCASTER_HEADER               production
  echo "<PAYLOAD>"   | /opt/homebrew/bin/npx vercel env add FARCASTER_PAYLOAD              production
  echo "<SIGNATURE>" | /opt/homebrew/bin/npx vercel env add FARCASTER_SIGNATURE            production
  echo "<0xADDRESS>" | /opt/homebrew/bin/npx vercel env add NEXT_PUBLIC_BASE_BUILDER_ADDRESS production
  /opt/homebrew/bin/npx vercel --prod --yes --scope simpl3s-projects

Verify after redeploy:

  curl -s https://mas-hillclimb.vercel.app/.well-known/farcaster.json \
    | jq '.accountAssociation, .baseBuilder'

Expected:
  { "header": "...", "payload": "...", "signature": "..." }
  { "allowedAddresses": ["0x..."] }

───────────────────────────────────────────────────────────────

After all 20 are signed and redeployed, verify in bulk:

  for g in 2048 wordle snake minesweeper sudoku pong clicker breakout \
           bubble solitaire match3 flappy crossy helix geometry jetpack \
           stickman tower pool hillclimb; do
    result=$(curl -s "https://mas-$g.vercel.app/.well-known/farcaster.json" \
      | python3 -c 'import sys,json
d=json.load(sys.stdin)
aa = isinstance(d.get("accountAssociation"),dict)
bb = d.get("baseBuilder",{}).get("allowedAddresses",[])
print("ok" if aa and bb else "missing")')
    echo "$g: $result"
  done

Expect: 20 × "ok".

Tip: env vars propagate to new deployments only. The curl above will
show the old manifest until the redeploy finishes. Vercel builds are
~2-3 min cold, ~30s warm (build cache reused).
