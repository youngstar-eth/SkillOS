{
  "name": "@skillos/engines",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, game-agnostic replay/adjudication engines for SkillOS (Δ6). Pure functions over a seeded LCG — no MCP, Next, or network deps. registry[gameId].verify(seed, inputLog) → { score, valid }.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./2048": "./src/games/game2048.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/games/__tests__/*.test.ts src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
